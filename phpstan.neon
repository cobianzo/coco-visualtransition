parameters:
  level: 5
  paths:
    - coco-visualtransition.php
    - inc/
  ignoreErrors:
    # Here the rules I want to ignore...
  bootstrapFiles:
    - phpstan-bootstrap.php

includes:
  # no need to include it because we use composer.
  # - vendor/szepeviktor/phpstan-wordpress/extension.neon
